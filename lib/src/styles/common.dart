import 'package:flutter/material.dart';

const kUserName = TextStyle(fontSize: 14, fontWeight: FontWeight.bold);
const kLocation = TextStyle(fontSize: 12, fontWeight: FontWeight.normal);

const kCardBorderRadius = BorderRadius.only(
  topLeft: Radius.circular(10.0),
  topRight: Radius.circular(10.0),
  bottomLeft: Radius.circular(10.0),
  bottomRight: Radius.circular(10.0),
);

const kPostActionButtonText = TextStyle(fontSize: 11);
const kIconSize = 22.0;
